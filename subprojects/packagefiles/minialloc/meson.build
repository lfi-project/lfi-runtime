project('minialloc', 'c')

# Source files only; the actual build happens in the parent project so the
# files can be compiled with the LFI cross-compiler for the sandbox.
minialloc_srcs = files(
  'alloc.c',
  'brk.c',
)
